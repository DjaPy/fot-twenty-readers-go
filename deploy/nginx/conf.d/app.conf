upstream app_backend {
    server app:8080 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

server {
    listen 80;
    server_name _;

    # HTTP Basic Authentication
    auth_basic "Restricted Access";
    auth_basic_user_file /etc/nginx/auth/.htpasswd;

    # Custom error pages
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;

    # Intercept backend errors and show custom pages
    proxy_intercept_errors on;

    # Serve custom error pages
    location = /404.html {
        root /usr/share/nginx/html;
        internal;
    }

    location = /50x.html {
        root /usr/share/nginx/html;
        internal;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # For local development - proxy to app
    # Comment this out in production and uncomment redirect below
    location / {
        limit_req zone=general_limit burst=20 nodelay;

        proxy_pass http://app_backend;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";

        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;

        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
    }

    # Uncomment for production (redirect HTTP to HTTPS)
    # location / {
    #     return 301 https://$host$request_uri;
    # }
}

# HTTPS server - uncomment for production with SSL certificates
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     ssl_certificate /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#
#     # HTTP Basic Authentication
#     auth_basic "Restricted Access";
#     auth_basic_user_file /etc/nginx/auth/.htpasswd;
#
#     # Modern SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
#     ssl_prefer_server_ciphers off;
#
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     ssl_session_tickets off;
#
#     # HSTS (uncomment after testing)
#     # add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     # Custom error pages
#     error_page 404 /404.html;
#     error_page 500 502 503 504 /50x.html;
#
#     # Intercept backend errors and show custom pages
#     proxy_intercept_errors on;
#
#     # Serve custom error pages
#     location = /404.html {
#         root /usr/share/nginx/html;
#         internal;
#     }
#
#     location = /50x.html {
#         root /usr/share/nginx/html;
#         internal;
#     }
#
#     location / {
#         limit_req zone=general_limit burst=20 nodelay;
#
#         proxy_pass http://app_backend;
#         proxy_http_version 1.1;
#
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Connection "";
#
#         proxy_connect_timeout 30s;
#         proxy_send_timeout 30s;
#         proxy_read_timeout 30s;
#
#         proxy_buffering on;
#         proxy_buffer_size 4k;
#         proxy_buffers 8 4k;
#         proxy_busy_buffers_size 8k;
#     }
#
#     # API endpoints with stricter rate limiting
#     location ~ ^/api/ {
#         limit_req zone=api_limit burst=5 nodelay;
#
#         proxy_pass http://app_backend;
#         proxy_http_version 1.1;
#
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Connection "";
#     }
#
#     # Static files caching (if any)
#     location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#         proxy_pass http://app_backend;
#         proxy_cache_valid 200 7d;
#         add_header Cache-Control "public, max-age=604800, immutable";
#     }
# }